pipeline {
  agent {
    node 'server'
  }

  stages {
    stage ('Backup') {
      
      steps {
        
        script {
          echo 'Backup complete'
        }
        
      }
      
    }
  }
}
